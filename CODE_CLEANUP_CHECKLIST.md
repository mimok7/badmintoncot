# 전체 페이지 코드 정리 체크리스트

이 문서는 모든 페이지와 공통 코드, Supabase SQL을 수정할 때 반드시 적용한다.
목표는 사용하지 않는 코드 제거와 중복 통합이며, 기존 기능·데이터·권한·화면 동작을 유지하는 것을 전제로 한다.

## 0. 변경 전 공통 확인

- [ ] 현재 라우트와 실제 기능을 확인한다: `/`, `/scan`, `/admin`, `/admin/login`.
- [ ] 변경 대상 코드의 호출처, DB 테이블/컬럼, RLS 정책, 브라우저 저장 키를 먼저 검색한다.
- [ ] 삭제 대상이 정말 미사용인지 `rg`로 import, 호출, 라우트, 이벤트 연결 여부를 확인한다.
- [ ] 기능 변경이 아닌 정리 작업은 한 번에 한 영역씩 작게 변경한다.
- [ ] 기존 기능의 정상 동작을 확인할 수 있는 기준을 변경 전에 기록한다.
- [ ] 사용자에게 보이는 문구는 한국어로 유지하고, `경기`와 `게임` 용어를 구분한다.
- [ ] 기존 작업 중인 변경사항을 덮어쓰지 않는다.

## 1. 모든 페이지 공통 점검

- [ ] 중복된 타입, 상수, 라벨, 상태 문구, 색상 매핑, 날짜/시간 포맷 함수를 공통 모듈로 통합한다.
- [ ] 동일한 Supabase 조회·저장·삭제 로직을 재사용 가능한 함수 또는 모듈로 통합한다.
- [ ] `any`는 실제 타입으로 교체하고, 불가피한 외부 라이브러리 타입에는 이유를 남긴다.
- [ ] 사용하지 않는 import, state, handler, interface, 상수, CSS class, 주석, 디버그 UI를 제거한다.
- [ ] `console.log`는 운영 코드에서 제거하거나 필요한 오류 로깅으로 제한한다.
- [ ] `useEffect`의 의존성 배열, cleanup, 중복 실행 여부를 확인한다.
- [ ] `setInterval`, `setTimeout`, `addEventListener`, `watchPosition`, Supabase 구독은 모두 cleanup한다.
- [ ] 로딩·성공·실패·빈 데이터·네트워크 오류 상태가 기존과 동일하게 처리되는지 확인한다.
- [ ] 버튼 중복 클릭, 빠른 페이지 이동, 새로고침, 모바일 화면에서의 동작을 확인한다.

## 2. `/` 홈·코트 현황 페이지

- [ ] 회원/입장 세션/코트/예약/구장/설정 조회가 중복 호출되지 않는지 확인한다.
- [ ] 구장 선택, 위치 권한, 지오펜스 거리 계산, 입장 만료 타이머의 동작을 변경하지 않는다.
- [ ] `localStorage` 키(`badminton_member_id`, 닉네임, 클럽명, PWA 상태)를 공통 상수로 통합한다.
- [ ] 알림 권한 요청과 PWA 설치 이벤트가 최초 1회 정책 및 cleanup을 유지하는지 확인한다.
- [ ] 예약·대기팀·취소·퇴장 처리에서 현재 사용자와 다른 사용자의 데이터가 섞이지 않는지 확인한다.
- [ ] 실시간 또는 주기적 현황 갱신 시 중복 타이머와 오래된 상태 반영을 점검한다.
- [ ] 클럽 색상, 상태 배지, 시간 표시 등 반복 UI를 공통 컴포넌트로 통합할지 검토한다.

## 3. `/scan` QR 입장 페이지

- [ ] QR 세션 검증, 기존 회원 복구, 신규 닉네임 등록, 입장 세션 생성 순서를 유지한다.
- [ ] 위치 권한 확인과 `watchPosition` cleanup을 확인한다.
- [ ] QR 세션 오류, 위치 밖 접근, 닉네임 누락, 재입장 제한, DB 오류 메시지를 각각 확인한다.
- [ ] 홈 페이지와 중복된 회원 식별·입장 시간 계산·구장 설정 조회 로직을 공통 함수로 통합한다.
- [ ] `localStorage` 회원 ID와 DB 회원의 불일치 시 정리 동작을 유지한다.
- [ ] 처리 완료 후 이동 타이밍과 뒤로가기/새로고침 상황을 확인한다.

## 4. `/admin/login` 관리자 로그인 페이지

- [ ] Supabase Auth 로그인, 세션 확인, 관리자 권한 확인, 리다이렉트 흐름을 유지한다.
- [ ] 오류 객체를 무조건 `any`로 받지 않고 안전한 오류 메시지 처리로 통합한다.
- [ ] 로그인 버튼 중복 제출과 로딩 해제 누락을 확인한다.
- [ ] `setTimeout` 리다이렉트가 컴포넌트 해제 후 실행되지 않도록 cleanup한다.
- [ ] 비밀번호·토큰·서비스 키가 로그나 클라이언트 코드에 노출되지 않는지 확인한다.

## 5. `/admin` 관리자 페이지

- [ ] 관리자·구장·코트·설정·클럽·예약·입장 세션 조회 로직의 중복을 정리한다.
- [ ] 각 데이터의 타입을 정의하고 `any[]`, `catch (err: any)`, 지도 이벤트의 `any`를 단계적으로 제거한다.
- [ ] 선택 구장 변경 시 이전 구장의 코트·예약·설정·통계가 남지 않는지 확인한다.
- [ ] 구장/코트/클럽 CRUD, 기본 코트 생성, 관리자 추가/삭제/수정의 성공·실패 상태를 확인한다.
- [ ] Leaflet 지도 인스턴스와 marker를 React ref 또는 명시적인 lifecycle로 관리하고 전역 `window` 객체 의존을 줄인다.
- [ ] 지도 초기화 지연 타이머, 클릭 이벤트, marker, DOM 참조를 모두 cleanup한다.
- [ ] QR 생성·복사·설정 저장·복원 기능의 기존 동작을 유지한다.
- [ ] 관리자 권한 없는 사용자 접근 차단 및 세션 만료 처리를 확인한다.

## 6. 공통 컴포넌트·레이아웃

- [ ] `QrScanner`의 카메라 정지, 타이머, 오류 콜백 cleanup을 확인한다.
- [ ] `BadmintonIcon` 등 단순 표시 컴포넌트는 중복 SVG/UI 구현과 비교해 하나로 통합한다.
- [ ] Service Worker 등록은 한 곳에서만 실행하며 성공 로그는 운영 환경에서 제거한다.
- [ ] metadata, manifest, 아이콘, 한국어 문서, 접근성 속성이 실제 동작과 일치하는지 확인한다.

## 7. Supabase SQL·권한 점검

- [ ] 모든 `public` 테이블에 RLS가 활성화되어 있는지 확인한다.
- [ ] `TO authenticated`만으로 권한을 부여하지 말고 실제 관리자/구장 소유 범위를 정책에 반영한다.
- [ ] `admin_users`의 `using (true)`와 `with check (true)`는 기능 보존 테스트 후 최소 권한 정책으로 검토한다.
- [ ] UPDATE 정책에는 `USING`과 `WITH CHECK`를 모두 확인한다.
- [ ] Auth 사용자 권한 판단에 `user_metadata`를 사용하지 않는다.
- [ ] SQL 파일 간 중복 `CREATE POLICY`, trigger, constraint, schema 변경 순서를 확인한다.
- [ ] 통계 trigger가 구장별 unique constraint와 충돌하지 않는지 insert/update 경쟁 상황을 확인한다.
- [ ] migration을 적용할 때 기존 데이터, FK, sequence, unique constraint, RLS 정책의 영향 범위를 확인한다.

## 8. 변경 후 검증

- [ ] `npm run type-check` 통과
- [ ] `npm run build` 통과
- [ ] 실제 개발 서버에서 네 페이지 접속 확인
- [ ] QR 신규 입장/기존 회원 입장/위치 제한/입장 만료 확인
- [ ] 홈에서 구장·코트·대기팀·예약·취소·퇴장 확인
- [ ] 관리자 로그인 및 관리자 페이지 CRUD 확인
- [ ] 모바일 브라우저에서 카메라·위치·알림·PWA 동작 확인
- [ ] Supabase RLS 정책을 anon/authenticated/관리자/다른 구장 사용자별로 확인
- [ ] 정리 후 삭제한 코드가 다시 참조되지 않는지 최종 `rg` 검색
- [ ] 변경 내역에 “삭제한 코드”, “통합한 코드”, “기능 검증 결과”, “남은 위험”을 기록

## 10. Git·배포 브랜치 확인

- [ ] 작업 시작 전에 `git branch --show-current` 결과가 `main`인지 확인한다.
- [ ] 커밋 전 `git status --short`로 의도하지 않은 파일이 없는지 확인한다.
- [ ] push 명령은 `git push origin main`을 사용한다.
- [ ] `master` 브랜치를 생성하거나 push하지 않는다.
- [ ] push 후 `git ls-remote --heads origin main`으로 원격 반영을 확인한다.
- [ ] 배포는 `main` push를 기준으로 실행한다.

## 9. 모든 향후 수정에 대한 필수 규칙

새 기능 추가, 버그 수정, UI 수정, SQL 수정, 리팩터링을 포함한 모든 변경은 다음 순서로 진행한다.

1. 변경 전 위 체크리스트의 관련 항목을 확인한다.
2. 미사용 코드와 중복 코드를 먼저 확인한다.
3. 기존 기능의 기준 동작을 기록한다.
4. 최소 범위로 수정한다.
5. 타입 검사·빌드·관련 기능을 검증한다.
6. 기능상 문제가 없음을 확인한 뒤에만 정리 작업을 완료한다.

기능 보존을 증명할 수 없는 코드는 삭제하거나 통합하지 않는다. 권한·데이터·입장·예약 관련 변경은 일반 코드 정리보다 우선하여 별도로 검토한다.

## 이번 정리 작업 기록

- [x] 미사용 일회성 루트 스크립트 6개 제거
- [x] 중복된 지오펜싱 거리 계산을 `lib/geo.ts`로 통합
- [x] 미사용 import·state·handler·지역 변수를 제거하고 TypeScript 미사용 검사 활성화
- [x] 존재하지 않는 npm 스크립트 제거 및 ESLint를 비대화형 실행 방식으로 정리
- [x] deprecated `auth.role()` 정책 구문을 `TO authenticated` 구문으로 정리
- [x] 타입 검사, ESLint, production build 완료
- [ ] 실제 Supabase RLS/CRUD 검증: 연결된 Supabase MCP 권한 부족으로 별도 실행 필요
- [ ] 모바일 카메라·위치·알림·PWA 실기기 검증
